import * as React from 'react';
import { View, Text } from 'react-native';

const NotesScreen = (props) => {
  return (
    <View>
      <Text>NotesScreen TAB ONE</Text>
    </View>
  );
};

export default NotesScreen;
